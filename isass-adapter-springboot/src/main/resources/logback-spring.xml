<!-- SPDX-License-Identifier: LGPL-3.0-only -->

<!--scan:默认为true。作用：当设置为true时，如果配置文件发生改变，将会被重新加载。-->
<!--scanPeriod：默认时间间隔为1分钟，默认单位是毫秒，当scan为true时，此属性生效。作用：检测配置文件是否有修改时间间隔。-->
<!--debug：默认为false。作用：当此属性为true时，将打印出logback内部日志信息，实时查看logback运行状态。-->
<configuration scan="false" scanPeriod="1 minutes">
    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <!-- 定义日志文件的存储地址 勿在 LogBack 的配置中使用相对路径 -->
    <property name="LOG_HOME" value="./logs"/>

    <!-- 定义日志内容格式 -->
    <!-- %d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符；
    不要在日志级别加颜色，避免 docker 日志收集到文件时出现乱码-->
    <property name="LOG_PATTERN"
              value="%d{MM-dd HH:mm:ss.SSS}[%thread]%-5level %-40.40logger{39} - %msg%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <!-- 输出到控制台和文件  -->
    <condition class="ch.qos.logback.core.boolex.ExpressionPropertyCondition">
        <expression>!propertyEquals("WRITE_LOG_STDOUT", "false") &amp;&amp; propertyEquals("WRITE_LOG_TO_FILE", "true")
        </expression>
    </condition>
    <if>
        <then>
            <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
                <encoder>
                    <pattern>${LOG_PATTERN}</pattern>
                </encoder>
            </appender>

            <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                    <fileNamePattern>${LOG_HOME}/log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                    <maxFileSize>100MB</maxFileSize>
                    <maxHistory>30</maxHistory>
                    <totalSizeCap>10GB</totalSizeCap>
                </rollingPolicy>

                <encoder>
                    <pattern>${LOG_PATTERN}</pattern>
                </encoder>
            </appender>

            <appender name="FILE_WARN" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                    <fileNamePattern>${LOG_HOME}/warn-log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                    <maxFileSize>100MB</maxFileSize>
                    <maxHistory>30</maxHistory>
                    <totalSizeCap>10GB</totalSizeCap>
                </rollingPolicy>

                <encoder>
                    <pattern>${LOG_PATTERN}</pattern>
                </encoder>

                <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                    <level>WARN</level>
                </filter>
            </appender>

            <root level="INFO">
                <appender-ref ref="STDOUT"/>
                <appender-ref ref="FILE_ALL"/>
                <appender-ref ref="FILE_WARN"/>
            </root>
        </then>
    </if>

    <!-- 只输出到控制台 -->
    <condition class="ch.qos.logback.core.boolex.ExpressionPropertyCondition">
        <expression>!propertyEquals("WRITE_LOG_STDOUT", "false") &amp;&amp; !propertyEquals("WRITE_LOG_TO_FILE", "true")
        </expression>
    </condition>
    <if>
        <then>
            <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
                <encoder>
                    <pattern>${LOG_PATTERN}</pattern>
                </encoder>
            </appender>

            <root level="INFO">
                <appender-ref ref="STDOUT"/>
            </root>
        </then>
    </if>

    <!-- 只输出到文件 -->
    <condition class="ch.qos.logback.core.boolex.ExpressionPropertyCondition">
        <expression>propertyEquals("WRITE_LOG_STDOUT", "false") &amp;&amp; propertyEquals("WRITE_LOG_TO_FILE", "true")
        </expression>
    </condition>
    <if>
        <then>
            <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                    <fileNamePattern>${LOG_HOME}/log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                    <maxFileSize>100MB</maxFileSize>
                    <maxHistory>30</maxHistory>
                    <totalSizeCap>10GB</totalSizeCap>
                </rollingPolicy>

                <encoder>
                    <pattern>${LOG_PATTERN}</pattern>
                </encoder>
            </appender>

            <appender name="FILE_WARN" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                    <fileNamePattern>${LOG_HOME}/warn-log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                    <maxFileSize>100MB</maxFileSize>
                    <maxHistory>30</maxHistory>
                    <totalSizeCap>10GB</totalSizeCap>
                </rollingPolicy>

                <encoder>
                    <pattern>${LOG_PATTERN}</pattern>
                </encoder>

                <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                    <level>WARN</level>
                </filter>
            </appender>

            <root level="INFO">
                <appender-ref ref="FILE_ALL"/>
                <appender-ref ref="FILE_WARN"/>
            </root>
        </then>
    </if>

    <!-- 都不输出 -->
    <condition class="ch.qos.logback.core.boolex.ExpressionPropertyCondition">
        <expression>propertyEquals("WRITE_LOG_STDOUT", "false") &amp;&amp; !propertyEquals("WRITE_LOG_TO_FILE", "true")
        </expression>
    </condition>
    <if>
        <then>
            <root level="INFO"/>
        </then>
    </if>
</configuration>