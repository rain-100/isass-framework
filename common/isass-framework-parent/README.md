基于 isass-framework 开发的服务，可以直接在pom通过parent的方式引入 
```xml
    <parent>
        <groupId>vip.isass</groupId>
        <artifactId>isass-framework-parent</artifactId>
        <version>${version}</version>
    </parent>
```