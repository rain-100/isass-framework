- [x]  模块重新命名，isass-framework 改为 isass。只修改模块名称和文件夹名称，不用修改包路径，包路径保持为vip.isass.framework.xxx

| 原模块名              | 新模块名    |
| --------------------- | :---------- |
| isass-framework-build | isass-build |

- [x] 新增模块：isass-adapter-springboot
  - 2026-06-20：完成第一阶段，`isass-adapter-springboot` 已成为 Spring Boot 运行时适配入口。`isass-core-common` 的 Spring Boot auto-configuration imports 已移除。后续目标是通过 Java SPI/classpath 条件承接 core 与功能模块的 Spring Boot 装配，使 `isass-core-*` 必须解耦 Spring，其他 `isass-*` 尽全力解耦 Spring。核心模块与 Spring 完全解耦仍归属 2024 L16 后续任务。
